image: node:latest

pages:
  script:
    - curl -f https://get.pnpm.io/v6.js | node - add --global pnpm@6
    - npm install
    - ./node_modules/.bin/gatsby build --prefix-paths
  artifacts:
    paths:
      - public
  only:
    - main
